<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>403 — Accès interdit · BTS SIO</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display&family=DM+Sans:wght@300;400;500&display=swap" rel="stylesheet" />
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --navy:   #0d1b2a;
      --blue:   #1a56db;
      --sky:    #e8f0fe;
      --line:   #d1dce8;
      --muted:  #6b7f96;
      --white:  #ffffff;
      --accent: #d97706;   /* amber — évoque une restriction / avertissement */
      --code-bg:#f3f6fb;
    }

    html, body {
      height: 100%;
      font-family: 'DM Sans', sans-serif;
      background: var(--white);
      color: var(--navy);
    }

    .shell {
      min-height: 100vh;
      display: grid;
      grid-template-rows: auto 1fr auto;
    }

    /* ── Header ── */
    header {
      border-bottom: 1px solid var(--line);
      padding: 0 2.5rem;
      height: 64px;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }

    .logo-wrap {
      display: flex;
      align-items: center;
      gap: .75rem;
      text-decoration: none;
      color: inherit;
    }

    .logo-icon {
      width: 38px; height: 38px;
      background: var(--blue);
      border-radius: 8px;
      display: flex; align-items: center; justify-content: center;
      flex-shrink: 0;
    }
    .logo-icon span {
      color: var(--white);
      font-size: .7rem;
      font-weight: 500;
      letter-spacing: .05em;
      line-height: 1.15;
      text-align: center;
    }

    .logo-label strong { display: block; font-size: .9rem; font-weight: 500; }
    .logo-label small  { font-size: .75rem; color: var(--muted); }

    .badge {
      font-size: .72rem;
      font-weight: 500;
      letter-spacing: .06em;
      color: #92400e;
      background: #fef3c7;
      border: 1px solid #fde68a;
      border-radius: 99px;
      padding: .25rem .75rem;
    }

    /* ── Main ── */
    main {
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 3rem 1.5rem;
    }

    .card {
      max-width: 680px;
      width: 100%;
    }

    /* Stamp */
    .stamp {
      display: inline-flex;
      align-items: baseline;
      gap: .5rem;
      margin-bottom: 1.5rem;
    }
    .stamp-code {
      font-family: 'DM Serif Display', serif;
      font-size: 6rem;
      line-height: 1;
      color: var(--navy);
      letter-spacing: -.03em;
    }
    .stamp-sep {
      width: 2px;
      height: 52px;
      background: var(--accent);
      border-radius: 2px;
      align-self: center;
    }
    .stamp-label {
      font-size: 1.1rem;
      font-weight: 300;
      color: var(--muted);
      line-height: 1.3;
      max-width: 140px;
    }

    h1 {
      font-family: 'DM Serif Display', serif;
      font-size: 1.85rem;
      font-weight: 400;
      line-height: 1.25;
      margin-bottom: .75rem;
    }

    .lead {
      font-size: 1rem;
      color: var(--muted);
      line-height: 1.65;
      margin-bottom: 2rem;
    }

    /* Alerte restriction */
    .alert {
      display: flex;
      gap: .75rem;
      align-items: flex-start;
      background: #fffbeb;
      border: 1px solid #fde68a;
      border-left: 4px solid var(--accent);
      border-radius: 8px;
      padding: 1rem 1.25rem;
      margin-bottom: 2rem;
      font-size: .88rem;
      line-height: 1.6;
      color: #78350f;
    }
    .alert svg { width: 18px; height: 18px; flex-shrink: 0; margin-top: .1rem; color: var(--accent); }

    /* Guide */
    .guide {
      border: 1px solid var(--line);
      border-radius: 12px;
      overflow: hidden;
      margin-bottom: 2rem;
    }
    .guide-header {
      background: var(--code-bg);
      border-bottom: 1px solid var(--line);
      padding: .65rem 1.25rem;
      font-size: .78rem;
      font-weight: 500;
      letter-spacing: .05em;
      color: var(--muted);
      text-transform: uppercase;
    }
    .guide-body { padding: 1.25rem 1.5rem; }

    .step {
      display: flex;
      gap: 1rem;
      align-items: flex-start;
      padding: .65rem 0;
    }
    .step + .step { border-top: 1px solid var(--line); }

    .step-num {
      width: 22px; height: 22px;
      background: var(--blue);
      color: var(--white);
      border-radius: 50%;
      display: flex; align-items: center; justify-content: center;
      font-size: .7rem;
      font-weight: 500;
      flex-shrink: 0;
      margin-top: .1rem;
    }
    .step-text strong { display: block; font-size: .88rem; font-weight: 500; margin-bottom: .1rem; }
    .step-text span   { font-size: .83rem; color: var(--muted); line-height: 1.5; }

    /* Actions */
    .actions {
      display: flex;
      flex-wrap: wrap;
      gap: .75rem;
      margin-bottom: 2rem;
    }

    .btn {
      display: inline-flex;
      align-items: center;
      gap: .45rem;
      padding: .6rem 1.3rem;
      border-radius: 8px;
      font-size: .88rem;
      font-weight: 500;
      text-decoration: none;
      transition: background .15s, color .15s, border-color .15s;
      cursor: pointer;
      border: none;
    }
    .btn-primary { background: var(--blue); color: var(--white); }
    .btn-primary:hover { background: #1446b8; }
    .btn-outline { background: transparent; color: var(--navy); border: 1px solid var(--line); }
    .btn-outline:hover { background: var(--sky); border-color: #b0c4de; }
    .btn svg { width: 15px; height: 15px; }

    /* Contact */
    .contact-bar {
      display: flex;
      align-items: center;
      gap: .6rem;
      font-size: .83rem;
      color: var(--muted);
      border-top: 1px solid var(--line);
      padding-top: 1.5rem;
    }
    .contact-bar svg { width: 16px; height: 16px; flex-shrink: 0; }
    .contact-bar a { color: var(--blue); text-decoration: none; font-weight: 500; }
    .contact-bar a:hover { text-decoration: underline; }

    /* Footer */
    footer {
      border-top: 1px solid var(--line);
      padding: 1rem 2.5rem;
      display: flex;
      align-items: center;
      justify-content: space-between;
      flex-wrap: wrap;
      gap: .5rem;
      font-size: .75rem;
      color: var(--muted);
    }

    @media (max-width: 540px) {
      .stamp-code { font-size: 4rem; }
      header { padding: 0 1rem; }
      footer  { padding: 1rem; flex-direction: column; text-align: center; }
    }
  </style>
</head>
<body>
<div class="shell">

  <!-- Header -->
  <header>
    <a class="logo-wrap" href="/">
      <div class="logo-icon">
        <span>BTS<br>SIO</span>
      </div>
      <div class="logo-label">
        <strong>Paul-Louis Courier</strong>
        <small>Hébergement de portfolios</small>
      </div>
    </a>
    <span class="badge">Accès restreint</span>
  </header>

  <!-- Main -->
  <main>
    <div class="card">

      <div class="stamp">
        <span class="stamp-code">403</span>
        <span class="stamp-sep"></span>
        <span class="stamp-label">Accès interdit</span>
      </div>

      <h1>Vous n'avez pas la permission d'accéder à cette ressource.</h1>
      <p class="lead">
        Le serveur a bien reçu votre requête, mais votre accès à cette page ou à ce répertoire
        est explicitement refusé par les règles de sécurité en vigueur.
      </p>

      <!-- Guide -->
      <div class="guide">
        <div class="guide-header">Que faire maintenant ?</div>
        <div class="guide-body">
          <div class="step">
            <span class="step-num">1</span>
            <div class="step-text">
              <strong>Vérifiez vos droits</strong>
              <span>Êtes-vous bien autorisé à consulter cette ressource ? Certains dossiers sont réservés aux administrateurs ou aux propriétaires du portfolio.</span>
            </div>
          </div>
          <div class="step">
            <span class="step-num">2</span>
            <div class="step-text">
              <strong>Retournez à l'accueil</strong>
              <span>Naviguez vers la page d'accueil du serveur pour accéder aux portfolios publiquement disponibles.</span>
            </div>
          </div>
          <div class="step">
            <span class="step-num">3</span>
            <div class="step-text">
              <strong>Demandez une autorisation</strong>
              <span>Si vous devez accéder à cette ressource dans le cadre de votre travail, contactez le propriétaire du portfolio ou l'administrateur du site.</span>
            </div>
          </div>
          <div class="step">
            <span class="step-num">4</span>
            <div class="step-text">
              <strong>Signalez une erreur de configuration</strong>
              <span>Si vous pensez que cet accès vous est dû et qu'il s'agit d'une erreur, contactez l'administrateur du site.</span>
            </div>
          </div>
        </div>
      </div>

      <!-- Actions -->
      <div class="actions">
        <a class="btn btn-primary" href="https://www.bts-sio.eu">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg>
          Accueil
        </a>
        <button class="btn btn-outline" onclick="history.back()">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="15 18 9 12 15 6"/></svg>
          Page précédente
        </button>
      </div>

      <!-- Contact -->
      <div class="contact-bar">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/><polyline points="22,6 12,13 2,6"/></svg>
        Un problème de droits ou une erreur ?&nbsp;<a href="mailto:contact@bts-sio.eu">contact@bts-sio.eu</a>
      </div>

    </div>
  </main>

  <!-- Footer -->
  <footer>
    <span>BTS SIO · Lycée Paul-Louis Courier</span>
    <span>Plateforme d'hébergement de portfolios étudiants</span>
  </footer>

</div>
</body>
</html>
